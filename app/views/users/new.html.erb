<p>Create a new user</p>

<%= form_for(@user) do |f| %>
  <%= f.label :name %>:<br>
  <%= f.text_field :name %><br><br>

  <%= f.submit %>
<% end %>

<%= link_to "Already a user? Login here", login_path %>

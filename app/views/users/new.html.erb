<p>Create a new user</p>

<%= form_for(@user) do |f| %>
  <%= render "shared/error_messages", object: f.object %> 

  <%= f.label :name %>:<br>
  <%= f.text_field :name %><br><br>

  <%= f.submit %>
<% end %>

